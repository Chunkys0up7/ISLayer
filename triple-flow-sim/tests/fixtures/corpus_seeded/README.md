# corpus_seeded

8 triples (3 clean + 5 defective) for integration testing of the inventory.
Each defective triple is designed to trigger one specific defect_class so the
integration test can assert each class appears.
